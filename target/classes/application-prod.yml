spring:
  datasource:
    url: jdbc:mysql://localhost:3306/courts_booking
    username: root
    password: Tonguyen24@

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration