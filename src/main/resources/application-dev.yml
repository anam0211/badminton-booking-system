spring:
  datasource:
    url: jdbc:mysql://localhost:3306/badmintin_final
    username: root
    password: "0211205"

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
